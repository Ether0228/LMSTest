name: Schoology Master Pipeline

on:
  schedule:
    - cron: '*/30 * * * *'   # 每 30 分钟运行一次

  # 手动触发：允许指定回溯日期和翻页数
  workflow_dispatch:
    inputs:
      target_date:
        description: '爬虫回溯日期 (YYYY-MM-DD)'
        required: false
        default: ''
      max_pages:
        description: '爬虫最大翻页数'
        required: false
        default: '2'

jobs:
  master-process:
    runs-on: ubuntu-latest
    timeout-minutes: 25

    steps:
      - name: 1. 检出代码
        uses: actions/checkout@v4

      - name: 2. 配置 Python 环境
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'

      - name: 3. 安装依赖库
        run: |
          python -m pip install --upgrade pip
          pip install requests selenium webdriver-manager gspread oauth2client

      # === 阶段一：爬虫抓取 ===
      - name: 4. 运行爬虫 (Scraper)
        env:
          FEISHU_APP_ID: ${{ secrets.FEISHU_APP_ID }}
          FEISHU_APP_SECRET: ${{ secrets.FEISHU_APP_SECRET }}
          FEISHU_APP_TOKEN: ${{ secrets.FEISHU_APP_TOKEN }}
          FEISHU_TABLE_ID: ${{ secrets.FEISHU_TABLE_ID }}
          FEISHU_ROSTER_TABLE_ID: ${{ secrets.FEISHU_ROSTER_TABLE_ID }}
          FEISHU_LIB_TABLE_ID: ${{ secrets.FEISHU_LIB_TABLE_ID }}
          SCHOOLOGY_COOKIES: ${{ secrets.SCHOOLOGY_COOKIES }}
          TZ: Asia/Shanghai
          TARGET_DATE: ${{ github.event.inputs.target_date }}
          MAX_PAGES: ${{ github.event.inputs.max_pages }}
        run: python "schoology数据转飞书/scraper_cloud_feishu.py"

      # === 阶段二：补全课程信息 ===
      - name: 5. 运行课程补全 (Fill Course Info)
        if: success()
        env:
          FEISHU_APP_ID: ${{ secrets.FEISHU_APP_ID }}
          FEISHU_APP_SECRET: ${{ secrets.FEISHU_APP_SECRET }}
          FEISHU_APP_TOKEN: ${{ secrets.FEISHU_APP_TOKEN }}
          FEISHU_LIB_TABLE_ID: ${{ secrets.FEISHU_LIB_TABLE_ID }}
          SCHOOLOGY_COOKIES: ${{ secrets.SCHOOLOGY_COOKIES }}
        run: python "schoology数据转飞书/fill_course_info.py"

      # === 阶段三：缺交核验 ===
      - name: 6. 运行缺交核验 (Check Missing)
        if: success()
        env:
          FEISHU_APP_ID: ${{ secrets.FEISHU_APP_ID }}
          FEISHU_APP_SECRET: ${{ secrets.FEISHU_APP_SECRET }}
          FEISHU_APP_TOKEN: ${{ secrets.FEISHU_APP_TOKEN }}
          FEISHU_TABLE_ID: ${{ secrets.FEISHU_TABLE_ID }}
          FEISHU_ROSTER_TABLE_ID: ${{ secrets.FEISHU_ROSTER_TABLE_ID }}
          FEISHU_LIB_TABLE_ID: ${{ secrets.FEISHU_LIB_TABLE_ID }}
          FEISHU_MISSING_TABLE_ID: ${{ secrets.FEISHU_MISSING_TABLE_ID }}
        run: python "schoology数据转飞书/check_missing_sync.py"

      # === 阶段四：构建学生汇总（仪表盘数据源）===
      - name: 7. 构建学生汇总 (Build Student Summary)
        if: success()
        env:
          FEISHU_APP_ID: ${{ secrets.FEISHU_APP_ID }}
          FEISHU_APP_SECRET: ${{ secrets.FEISHU_APP_SECRET }}
          FEISHU_APP_TOKEN: ${{ secrets.FEISHU_APP_TOKEN }}
          FEISHU_TABLE_ID: ${{ secrets.FEISHU_TABLE_ID }}
          FEISHU_ROSTER_TABLE_ID: ${{ secrets.FEISHU_ROSTER_TABLE_ID }}
          FEISHU_LIB_TABLE_ID: ${{ secrets.FEISHU_LIB_TABLE_ID }}
          FEISHU_MISSING_TABLE_ID: ${{ secrets.FEISHU_MISSING_TABLE_ID }}
          FEISHU_SUMMARY_TABLE_ID: ${{ secrets.FEISHU_SUMMARY_TABLE_ID }}
        run: python "schoology数据转飞书/build_student_summary.py"
